# dsh web 修复记录（2026-08-16：端口占用 + image-inbox 客户端两处修复）

> 供 dsh-collaboration 维护者阅读。本文记录今天对 DSH web profile 的诊断与修复，
> 以及遗留的待办项。

## 1. EADDRINUSE（已解决，非代码问题）

现象：`dsh web` 报 `listen EADDRINUSE: address already in use 127.0.0.1:3080`。

原因：有另一个 dsh web 实例还在运行占着 3080 端口（多次是测试/残留实例）。
处理：杀掉残留进程即可。**教训：重启 dsh 前先确认旧实例已退出**（`netstat -ano | findstr 3080`）。

## 2. image-inbox 客户端修复（已完成两部分，剩一步）

### 2.1 客户端 bundle 格式错误（已修复）

现象：浏览器控制台报
`client-modules: bundle .../client.js loaded without registering "@dsh-collaboration/tool-image-inbox" via __ModuleLoader__.load` + `Cannot use import statement outside a module`。

原因：`client.ts` 用 tsup `--format esm` 打出含 `import` 的 ES 模块，而 dsh 客户端模块加载器要求：
**经典脚本 + `window.__ModuleLoader__.load({ id, factory: (require) => {...} })` 自注册**，
外部依赖（react、@deepseek-ai/*）通过 factory 的 `require` 运行时解析。

修复：`packages/tools/tool-image-inbox/scripts/build-client.mjs`（新增）用 esbuild 打
`--format=cjs --platform=browser`，external `react`、`react/*`、`@deepseek-ai/*`，用
banner/footer 包裹成 `__ModuleLoader__.load` 格式；package.json 的 build 脚本改为
`tsup ... && node scripts/build-client.mjs`，devDependencies 加了 `esbuild ^0.27.7`。

### 2.2 宿主服务必须默认导出为 Service 类（已修复）

原因：官方 Remote 服务（dsh-goal 的 `GoalService`、dsh-commands 的 `CommandRuntime`）
都以 **default export 的 Service 类**形式注册——cordis 加载器把它实例化在**共享上下文**，
typert 网关的 `collectSrcClaims`/`resolveSrcDescriptor` 通过 `ctx.reflect.props` 扫描
`typertRemote` 绑定才能发现它。原来 `apply(ctx)` 里 `new ImageInboxService(ctx)` 注册在
插件私有 fork 上下文里，网关看不见。

修复：`src/index.ts` 删除 `apply`，改为 `export default ImageInboxService`
（类保留 `static inject = ['agents']`，`super(ctx, 'imageInbox')` 不变）。

### 2.3 剩余：客户端 Remote 命名空间挂载（待实现）

现象（当前状态）：控制台报
`web boot: 1 entry did not activate — @dsh-collaboration/tool-image-inbox: pending (waiting for service: remote.imageInbox)`。
**Web 应用其余部分正常**，只是「上传图片」按钮不出现。

机制（已逆向确认）：
- 客户端 `remote.<namespace>` 服务由 `dsh-api-gateway/lib/client.js` 的
  `ClientRemoteService.$mount(contribution)` 安装；namespace 未被 mount 前，
  inject `remote.imageInbox` 永远 pending。
- 官方包在构建期生成 `lib/typert.remote-client.js`（TYPERT_REMOTE contribution 对象，
  含严格 schemastery schema），dsh-api-remotes 的客户端 bundle 把它们全部 inline 并在
  apply 里逐个 `$mount`。
- 第三方包要做同样的事：**客户端模块里自己构造 contribution 并 `ctx.remote.$mount(...)`**。

待实现（建议在套件里做）：
1. `src/client.ts` 的 inject 去掉 `remote.imageInbox`（只留 `slots`、`remote`）。
2. apply 开头 `await ctx.remote.$mount(CONTRIBUTION)`，contribution 形状参照
   `dsh-api-remotes/lib/client.js` 里 commands 的 TYPERT_REMOTE$4：
   ```js
   {
     package: '@dsh-collaboration/tool-image-inbox',
     descriptors: [{
       id: '@dsh-collaboration/tool-image-inbox#imageInbox/upload',
       service: 'imageInbox',
       namespace: 'imageInbox',
       method: 'upload',
       invocation: { kind: 'direct' },
       scope: { context: 'agent', wire: 'agentId' },
       parameters: [
         { name: 'agent', wire: 'agentId', source: 'lookup', lookup: 'agent',
           codec: { mode: 'strict', schema: <agentId 的 schemastery schema> } },
         { name: 'input', wire: 'input', source: 'json',
           codec: { mode: 'strict', schema: z.object({ name: z.string(), mediaType: z.string(), data: z.string() }) } },
       ],
       result: { mode: 'strict', schema: z.union([...UploadResult 两种形状]) },
     }],
   }
   ```
   `requireStrictDescriptor` 强制所有参数与 result 都是 strict codec（带 schemastery schema）。
3. 调用约定：session 场景内 `ctx.remote.imageInbox.upload(input)`（agent 身份由
   'agent' context binder 从调用方 ctx 解析；参考 dsh-client-runtime Session 与
   ui-goal 客户端的写法，注意 scoped projection 的参数计数规则：业务参数数 =
   parameters.length - 1）。
4. schemastery 作为依赖加入该包（客户端 bundle 会把它 inline 进去）。

验证方式：`dsh web` 后打开页面，控制台应无 `web boot` 错误；`window.__ModuleLoader__`
已注册 `@dsh-collaboration/tool-image-inbox`；会话输入框工具行出现「上传图片」按钮。

## 3. 打包/安装要点（容易踩坑）

- tarball 输出：`pnpm --dir packages/tools/tool-image-inbox pack --pack-destination dist`
  会落到 `packages/tools/tool-image-inbox/dist/`（相对包目录），需手动复制到仓库根 `dist/`。
- profile 重装：直接 `pnpm install` 不会感知 tarball 内容变化（lockfile 缓存），
  必须 `pnpm install --force`（会报 checksum 不匹配并自动重新解析）。
- 客户端 bundle 改造后注意清掉 tsup 残留的 `lib/chunk-*.js`（多入口旧产物）。

## 4. 当前 dsh web 状态

- 宿主启动正常（~4 秒，HTTP 200），pi-ai 官方目录启用，settings.yaml 用 `zai` 路由；
- 已知唯一问题：2.3 的按钮未挂载（不影响其他功能）。
- 若暂时不想要该按钮：在 `cordis.patch.yml` 删除 `collaboration-image-inbox` 行即可。
