node_modules/
lib/
dist/
*.tsbuildinfo
*.log
.env
.env.local
.DS_Store
research/
.dsh/

# image-inbox uploads (transient paste artifacts)
.dsh-inbox/

# docker e2e artifacts (screenshots, console dumps)
.e2e-artifacts/

# local session archive export (user data, not project source)
dsh-agents-sessions.md

# temporary catalog-entry staging (belongs to the awesome list repo, not this one)
.tmp-catalog/
